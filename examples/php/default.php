<?php declare(strict_types=1);

// Type your code here, or load an example.

// Add the "-e" compiler option to see what the extra
// opcodes to "Generate extended information for debugger/profiler"

function add(int $a, int $b): int {
    return $a + $b;
}

echo add(1, 2);
